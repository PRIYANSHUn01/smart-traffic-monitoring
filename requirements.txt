# requirements.txt — pinned versions for reproducible, secure deployments
# VUL-12 FIX: Pinned exact versions to prevent unexpected upstream breakage
# and reduce supply-chain attack surface.
# To update: test with a new version, then update the pin here.

ultralytics==8.2.0
torch==2.3.1
torchvision==0.18.1
opencv-python==4.9.0.80
numpy==1.26.4
easyocr==1.7.1
streamlit==1.35.0
plotly==5.22.0
pandas==2.2.2
Pillow==10.3.0
tqdm==4.66.4
python-dotenv==1.0.1
matplotlib==3.9.0
pyyaml==6.0.1
pytest==8.2.0
fastapi==0.111.0
uvicorn[standard]==0.30.1
